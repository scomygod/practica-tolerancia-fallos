apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications
  namespace: tickets
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications
  template:
    metadata:
      labels:
        app: notifications
    spec:
      containers:
        - name: notifications
          image: tickets/notifications:1.0
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: tickets-config
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: tickets
spec:
  type: ClusterIP
  selector:
    app: notifications
  ports:
    - name: http
      port: 8000
      targetPort: http
